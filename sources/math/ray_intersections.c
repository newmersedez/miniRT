#include "../../includes/minirt.h"

float	ray_intersect_sphere(const void *data, const t_point *start_point,
			const t_vec *ray, t_point *intersection)
{
	(void)data;
	(void)start_point;
	(void)ray;
	(void)intersection;
	return (0);
}

float	ray_intersect_plane(const void *data, const t_point *start_point,
			const t_vec *ray, t_point *intersection)
{
	(void)data;
	(void)start_point;
	(void)ray;
	(void)intersection;
	return (0);
}

float	ray_intersect_cylinder(const void *data, const t_point *start_point,
			const t_vec *ray, t_point *intersection)
{
	(void)data;
	(void)start_point;
	(void)ray;
	(void)intersection;
	return (0);
}
